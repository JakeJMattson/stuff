title: Jake's Stuff

markup:
  goldmark:
    renderer:
      unsafe: true

module:
  imports:
    - path: github.com/imfing/hextra

caches:
  images:
    dir: ':cacheDir/images'

params:
  navbar:
    displayTitle: true
    displayLogo: false

menu:
  main:
    - name: Search
      weight: 9
      params:
        type: search
    - name: GitHub
      weight: 10
      url: "https://github.com/JakeJMattson"
      params:
        icon: github